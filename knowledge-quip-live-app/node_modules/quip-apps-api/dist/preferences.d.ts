import Client, { EventType } from "./client";
declare enum PreferenceType {
    USER = 0,
    SITE = 1
}
export default class Preferences {
    constructor(client?: Client, preferenceType?: PreferenceType, eventType?: EventType, rateLimiter?: any, // RateLimiter,
    sizeLimit?: {
        entries: number;
        size: number;
    });
    preferencesValue: {
        [key: string]: any;
    };
    clear(): void;
    getAll(): {
        [key: string]: any;
    };
    getForKey(key: string): any;
    save(prefs: {
        [key: string]: any;
    }): void;
}
export {};
//# sourceMappingURL=preferences.d.ts.map