"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.prettyPrintObject = exports.println = exports.print = void 0;
const tslib_1 = require("tslib");
const chalk_1 = tslib_1.__importDefault(require("chalk"));
exports.print = process.stdout.write.bind(process.stdout);
exports.println = console.log;
const ptabs = (count) => {
    let tabs = "";
    for (var i = 0; i < count; i++) {
        tabs += " ";
    }
    return tabs;
};
exports.prettyPrintObject = (obj, tabs = 0, prefix = "") => {
    if (!obj) {
        return;
    }
    else if (Array.isArray(obj)) {
        exports.print("\n");
        for (let i = 0; i < obj.length; i++) {
            const item = obj[i];
            exports.print(chalk_1.default `${prefix}${ptabs(tabs)}{grey [${i}]: }`);
            exports.prettyPrintObject(item, tabs + 1);
        }
    }
    else if (typeof obj === "object") {
        exports.print("\n");
        for (const key in obj) {
            exports.print(chalk_1.default `${prefix}${ptabs(tabs)}{grey - ${key}:} `);
            exports.prettyPrintObject(obj[key], tabs + 1);
        }
    }
    else {
        exports.print(chalk_1.default `${prefix}{green ${obj}}
`);
    }
};
