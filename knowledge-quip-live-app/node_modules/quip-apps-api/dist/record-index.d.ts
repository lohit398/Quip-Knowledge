import Record from "./record";
export default class RecordIndex<T extends Record> {
    idValue: string;
    entriesValue: T[];
    count(): number;
    id(): string;
    listen(listener: (recordIndex: RecordIndex<T>) => void): void;
    unlisten(listener: (recordIndex: RecordIndex<T>) => void): void;
}
//# sourceMappingURL=record-index.d.ts.map